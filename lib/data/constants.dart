import 'package:flutter/cupertino.dart';

class Constants {
  static const String baseUrl = 'https://jsonplaceholder.typicode.com/';
  static GlobalKey<NavigatorState> keys = GlobalKey(debugLabel: 'api_key');
}
